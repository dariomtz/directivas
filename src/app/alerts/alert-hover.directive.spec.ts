import { AlertHoverDirective } from './alert-hover.directive';

describe('AlertHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertHoverDirective();
    expect(directive).toBeTruthy();
  });
});
